# version: 1.1.0
# Tokyo Night Color Theme with Nerd Font Icons

add_newline = true
continuation_prompt = "[ ](dimmed white)"

# Tokyo Night Palette Reference:
# Red: #f7768e | Green: #9ece6a | Yellow: #e0af68
# Blue: #7aa2f7 | Magenta: #bb9af7 | Cyan: #7dcfff
# Foreground: #c0caf5 | Comment: #565f89

format = """($nix_shell$container$fill$git_metrics\n)$cmd_duration\
$hostname\
$localip\
$shlvl\
$shell\
$env_var\
$jobs\
$sudo\
$username\
$character"""

right_format = """
$singularity\
$kubernetes\
$directory\
$vcsh\
$fossil_branch\
$git_branch\
$git_commit\
$git_state\
$git_status\
$hg_branch\
$pijul_channel\
$docker_context\
$package\
$c\
$cpp\
$cmake\
$cobol\
$daml\
$dart\
$deno\
$dotnet\
$elixir\
$elm\
$erlang\
$fennel\
$fortran\
$golang\
$guix_shell\
$haskell\
$haxe\
$helm\
$java\
$julia\
$kotlin\
$gradle\
$lua\
$nim\
$nodejs\
$ocaml\
$opa\
$perl\
$php\
$pulumi\
$purescript\
$python\
$raku\
$rlang\
$red\
$ruby\
$rust\
$scala\
$solidity\
$swift\
$terraform\
$vlang\
$vagrant\
$xmake\
$zig\
$buf\
$conda\
$pixi\
$meson\
$spack\
$memory_usage\
$aws\
$gcloud\
$openstack\
$azure\
$crystal\
$custom\
$status\
$os\
$battery"""

[fill]
symbol = ' '

# ═══════════════════════════════════════════════════════════════════════════════
# PROMPT CHARACTER
# ═══════════════════════════════════════════════════════════════════════════════
[character]
format = "$symbol "
success_symbol = "[❯](bold #9ece6a)"
error_symbol = "[❯](bold #f7768e)"
vimcmd_symbol = "[](bold #7aa2f7)"
vimcmd_replace_one_symbol = "[](bold #bb9af7)"
vimcmd_replace_symbol = "[](bold #bb9af7)"
vimcmd_visual_symbol = "[󰒉](bold #e0af68)"

[env_var.VIMSHELL]
format = "[$env_value]($style)"
style = 'green italic'

# ═══════════════════════════════════════════════════════════════════════════════
# USER & SYSTEM
# ═══════════════════════════════════════════════════════════════════════════════
[sudo]
format = "[$symbol]($style) "
style = "bold #bb9af7"
symbol = ""
disabled = false

[username]
style_user = "bold #e0af68"
style_root = "bold #f7768e"
format = "[ $user]($style) "
disabled = false
show_always = false

[hostname]
format = "[󰒋 $hostname]($style) "
style = "bold #7dcfff"
ssh_only = true

[localip]
ssh_only = true
format = "[󰛳 $localipv4]($style) "
style = "bold #bb9af7"
disabled = false

[jobs]
format = "[ $number]($style) "
style = "#7aa2f7"
number_threshold = 1
symbol_threshold = 1

[cmd_duration]
format = "[ $duration]($style) "
style = "italic #565f89"
min_time = 2000

# ═══════════════════════════════════════════════════════════════════════════════
# DIRECTORY
# ═══════════════════════════════════════════════════════════════════════════════
[directory]
home_symbol = " ~"
truncation_length = 2
truncation_symbol = "…/"
read_only = " "
use_os_path_sep = true
style = "#7aa2f7"
read_only_style = "#f7768e"
format = '[$path]($style)[$read_only]($read_only_style)'
repo_root_style = 'bold #7aa2f7'
repo_root_format = '[$before_root_path]($before_repo_root_style)[$repo_root]($repo_root_style)[$path]($style)[$read_only]($read_only_style) [](bold #9ece6a)'

# ═══════════════════════════════════════════════════════════════════════════════
# GIT
# ═══════════════════════════════════════════════════════════════════════════════
[git_branch]
format = "[ $branch(:$remote_branch)]($style)"
symbol = ""
style = "#9ece6a"
truncation_symbol = "…"
truncation_length = 11
ignore_branches = ["main", "master"]
only_attached = true

[git_metrics]
format = '([ +$added]($added_style))([ -$deleted]($deleted_style))'
added_style = '#9ece6a'
deleted_style = '#f7768e'
ignore_submodules = true
disabled = false

[git_status]
style = "#7aa2f7"
format = '([$all_status$ahead_behind]($style))'
conflicted = " 󰞇"
ahead = " ⇡${count}"
behind = " ⇣${count}"
diverged = " ⇕⇡${ahead_count}⇣${behind_count}"
untracked = " "
stashed = " 󰏗"
modified = " "
staged = " [+${count}](#9ece6a)"
renamed = " 󰑕"
deleted = " "

# ═══════════════════════════════════════════════════════════════════════════════
# TIME & BATTERY (time disabled - macOS shows it)
# ═══════════════════════════════════════════════════════════════════════════════
[time]
disabled = true

[battery]
format = "[$symbol$percentage]($style) "
full_symbol = "󰁹 "
charging_symbol = "󰂄 "
discharging_symbol = "󰂃 "
unknown_symbol = "󰂑 "
empty_symbol = "󰂎 "

[[battery.display]]
threshold = 20
style = "bold #f7768e"

[[battery.display]]
threshold = 50
style = "#e0af68"

[[battery.display]]
threshold = 80
style = "#565f89"

# ═══════════════════════════════════════════════════════════════════════════════
# LANGUAGES & TOOLS - Nerd Font Icons
# ═══════════════════════════════════════════════════════════════════════════════
[nodejs]
format = "[ $version]($style) "
version_format = "${raw}"
style = "#9ece6a"
detect_files = ["package-lock.json", "yarn.lock", "package.json"]
detect_folders = ["node_modules"]
detect_extensions = ["js", "mjs", "cjs", "ts", "mts", "cts"]

[python]
format = "[ $version( $virtualenv)]($style) "
version_format = "${raw}"
style = "#e0af68"
symbol = ""

[rust]
format = "[ $version]($style) "
version_format = "${raw}"
style = "#f7768e"
symbol = ""

[golang]
format = "[ $version]($style) "
version_format = "${raw}"
style = "#7dcfff"
symbol = ""

[ruby]
format = "[ $version]($style) "
version_format = "${raw}"
style = "#f7768e"
symbol = ""

[lua]
format = "[ $version]($style) "
version_format = "${raw}"
style = "#7aa2f7"
symbol = ""

[swift]
format = "[ $version]($style) "
version_format = "${raw}"
style = "#f7768e"
symbol = ""

[java]
format = "[ $version]($style) "
version_format = "${raw}"
style = "#f7768e"
symbol = ""

[kotlin]
format = "[ $version]($style) "
version_format = "${raw}"
style = "#bb9af7"
symbol = ""

[deno]
format = "[🦕 $version]($style) "
version_format = "${raw}"
style = "#9ece6a"

[c]
format = "[ $version]($style) "
symbol = ""
style = "#7aa2f7"

[cpp]
format = "[ $version]($style) "
symbol = ""
style = "#7aa2f7"

[dart]
format = "[ $version]($style) "
symbol = ""
style = "#7dcfff"

[elixir]
format = "[ $version]($style) "
symbol = ""
style = "#bb9af7"

[elm]
format = "[ $version]($style) "
symbol = ""
style = "#7dcfff"

[haskell]
format = "[ $version]($style) "
symbol = ""
style = "#bb9af7"

[julia]
format = "[ $version]($style) "
symbol = ""
style = "#9ece6a"

[nim]
format = "[ $version]($style) "
symbol = ""
style = "#e0af68"

[zig]
format = "[ $version]($style) "
symbol = ""
style = "#e0af68"

[scala]
format = "[ $version]($style) "
symbol = ""
style = "#f7768e"

[perl]
format = "[ $version]($style) "
symbol = ""
style = "#7aa2f7"

[php]
format = "[ $version]($style) "
symbol = ""
style = "#bb9af7"

# ═══════════════════════════════════════════════════════════════════════════════
# PACKAGE MANAGERS & CONTAINERS
# ═══════════════════════════════════════════════════════════════════════════════
[package]
format = "[󰏗 $version]($style) "
version_format = "${raw}"
style = "#e0af68"
symbol = "󰏗"

[docker_context]
format = "[ $context]($style) "
symbol = ""
style = "#7dcfff"

[conda]
format = "[ $environment]($style) "
symbol = ""
style = "#9ece6a"

[pixi]
format = "[󰏗 $environment]($style) "
symbol = "󰏗"
style = "#9ece6a"

[buf]
format = "[󰿘 $version]($style) "
symbol = "󰿘"
style = "#7aa2f7"

# ═══════════════════════════════════════════════════════════════════════════════
# CLOUD & INFRASTRUCTURE
# ═══════════════════════════════════════════════════════════════════════════════
[aws]
disabled = true
format = "[ $profile($region)]($style) "
style = "#e0af68"
symbol = ""

[gcloud]
format = "[ $project]($style) "
style = "#7aa2f7"
symbol = ""

[azure]
format = "[󰠅 $subscription]($style) "
style = "#7aa2f7"
symbol = "󰠅"

[kubernetes]
format = "[󱃾 $context( $namespace)]($style) "
style = "#7aa2f7"
symbol = "󱃾"
disabled = false

[terraform]
format = "[󱁢 $workspace]($style) "
style = "#bb9af7"
symbol = "󱁢"

[vagrant]
format = "[ $version]($style) "
style = "#7aa2f7"
symbol = ""

[helm]
format = "[󰠳 $version]($style) "
style = "#7aa2f7"
symbol = "󰠳"

# ═══════════════════════════════════════════════════════════════════════════════
# NIX & SYSTEM
# ═══════════════════════════════════════════════════════════════════════════════
[nix_shell]
format = '[ $state( $name)]($style) '
symbol = ''
style = '#7dcfff'
impure_msg = 'impure'
pure_msg = 'pure'
unknown_msg = ''

[memory_usage]
format = "[󰍛 ${ram}]($style) "
symbol = "󰍛"
style = "#565f89"
threshold = 75
disabled = true

[spack]
format = "[󰏗 $environment]($style) "
symbol = "󰏗"
style = "#bb9af7"
